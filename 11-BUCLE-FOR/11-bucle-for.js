'use strict'
let i = 0; // Inicialización
let t = 5; // Condición
while (i < 5) {
    alert("Vuelta " + i);
    i++; // Incremento
}

// Inicialización, condición, incremento.
for (let i = 0; i < 5; i++) {
    alert("Vuelta con For " + i);
}
