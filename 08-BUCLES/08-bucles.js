'use strict';

// Declarar y asignar variable i.
let i = 0;
let t = Number(prompt("Ingrese el número de vueltas"));
while (i < t) {
    alert("Vuelta N. " + (Number(i) + 1));
    i++; // i = i + 1;
}
