'use strict'
let a = Number(prompt("Ingrese la variable a"));
let b = Number(prompt("\nIngrese la variable  b"));
// let a = 1; // Declarando la variable "a" y asignando 1.
// let b; // Declarando
// b = 10; // Asignando
let resultado;
// Almacenar en la variable "resultado" la suma de "a" y "b".
// Luego mostrar el contenido de "result"
resultado = a + b;
alert(resultado);
// Multiplicar los valores de "a" y "b". Almacenamos en "resultado".
resultado = a * b;
alert(resultado);
// Hallar el resto de "a" para "b".
resultado = a % b;

// Convertir una variable "a2" y asignar el valor de "a" en negativo.
// Mostrar "a2"
let a2 = -a;

// Incrementar el valor de "a" en uno.
a++; // a = a + 1;
alert(a);
// Decrementar el valor de "b" en uno.
b--;
alert(b);